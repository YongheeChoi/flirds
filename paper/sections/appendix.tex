% ==================== APPENDIX (stubs; sources mapped) ====================

\section{Proofs and Formal Statements}
\label{app:proofs}
\todo{Port P1--P8 full statements, assumptions (F1--F12), proofs, and
counterexamples from the math-rigor dossier
(\texttt{research-wiki/survey/irds-fl-math-rigor-2026-07/}). Includes: null-player
lemma L1; telescoping G1; merge-consistency L3 and its third-order failure
counterexample; P4 per-sample$\to$per-client bridge (CNN mean-CE 1-step limit;
LLM token-mean non-transfer remark); P5 fixed-vs-renormalized rank-flip
counterexamples ($3{:}1$ sizes; partial participation; curvature
amplification); P6 path-dependence and $R{=}1$ counterexample; P7 momentum
scoping (server statefulness is the load-bearing knob); P8 LoRA-coordinate
scope. GPT-2 smoke-test numerics table (closed form vs \texttt{flirds\_values}
$5.83\times 10^{-12}$; per-round vs $2^N$ $3.93\times 10^{-7}$; telescoping
$2.4$--$9.5\times 10^{-7}$).}

\section{Own-Game Decomposition and Oracle Adjudication (Judge Cell)}
\label{app:owngame}
\todo{E1 results. Design (implemented; awaiting GPU): 1B, $N{=}5$ non-IID
silos, $R{=}10$, fp32; equal-$n$ and unequal-$n$ ($\{600,300,150,150,100\}$)
variants $\times$ 3 seeds; per trajectory compute fixed-weight exact Shapley,
subset-renormalized exact Shapley, retrain oracle (a), all method scores,
ShapleyFL pre-EMA own-game values; persist all $2^N$ coalition utilities.}

\section{Full Fidelity Tables}
\label{app:tables}
\todo{Per-scale (1B/3B/7B) $\times$ per-stage tables with mean$\pm$std and 95\%
bootstrap CIs; Kendall/Pearson/distance-trio companions to every Spearman
headline; CNN per-scenario (10 scenarios) tables; cross-seed stability tables.
Source: \texttt{make\_fidelity.py} / \texttt{make\_analysis.py} regeneration
from run directories.}

\section{Prior-Work Taxonomy}
\label{app:taxonomy}
\todo{Port the 6-axis taxonomy and the two cross tables (federation $\times$
model class; federation $\times$ basis) from
\texttt{research-wiki/survey/prior-work-taxonomy/}, with the observed-gap
statement and its scope caveats (7 uncollected papers listed).}

\section{Protocol Details and Reproducibility}
\label{app:protocol}
\todo{Data layer (5-domain composition, sizes, stratified validation);
corruptor definitions (answer-swap; free-rider zero/random; scaled backdoor
$\gamma$, poison-frac); threat-matched detector configurations; hyperparameters
per stage; hardware; run-directory schema (config/meta/git-SHA/$\phi$-parquet/
metrics); known accounting caveats (loss-heuristic $\sim2\times$ overhead;
Ripple trajectory inclusion; CPU-based detectors); precision audit summary
(matmul fp32 verified; cuDNN-conv TF32 exposure on CNN track
\todo{TF32 A/B, E10}).}

\section{Signal-Size Diagnosis (Extended)}
\label{app:signal}
\todo{A-axis probes (rank/participation/lr/steps grids) and B-axis matrix full
tables; bootstrap-vs-spread noise analysis; MMLU power analysis.}

# 프로젝트: flirds

## Pipeline Status

```yaml
stage: implementation # idle | idea-discovery | implementation(/experiment-bridge) | training | review | paper
idea: "client-level FL Shapley via 1st+2nd Taylor of validation loss (Flirds)"  # 현재 idea 한 줄 요약
contract: "research-wiki/wiki/flirds-implementation-plan.md"  # operational plan (wiki 기반; 별도 research_contract 없음)
current_branch: "main"   # feature/flirds-phase-0 → main 병합 후 브랜치 삭제; main 직접 작업
baseline: "Valuation-baseline LLM 1B N=5 3-seed(2026-06-07): **ALL methods Spearman +1.000 vs (b)oracle** (Flirds·Flirds-1st·GTG·FedSV·Banzhaf·ShapleyFL·loss-heur). runtime Flirds-1st~35s/Flirds~107s/loss-heur~164s/GTG~537s/FedSV~532s/Banzhaf~531s/ShapleyFL~531s/Ripple~4515s/(b)oracle~531s; AUROC noisy0.75/FR1.0 (coarse@N=5; Ripple noisy0.50±0.20). free-rider φ exact-0: Flirds/oracle/Banzhaf/loss-heur (GTG/FedSV renorm≠0). N=5 near-additive→Flirds dominates frontier 5–15× cheaper. #7 selection works both lr. **Detection (task5 FLDetector, 06-07)**: model-free server-side L-BFGS detector = **cheapest ~24s** but **weakest** AUROC noisy0.50/FR0.75 (vs valuation 0.75/1.0) — clean math client tops suspicious score every seed = non-IID erosion (headline N=10/100). **Dual-oracle (task6, 06-07, N=5@1B fp32)**: (a)-retrain-val-loss = (b) in-run = estimator **Spearman +1.000** (method validated); (a)-ROUGE diverges (different game, corruption-fooled). [CNN Phase0: ComFedSV Spearman {1.0,0.96,0.85,0.84}] **Cross-device(task7,06-08,N=100 α=0.5,1B)**: Flirds vs exact-per-round (b) **Spearman +1.000**(near-additive holds at scale); oracle 771ms/fwd(fp32-B200, no-tensor-core)→~11h/4-GPU. **3B (a)-valloss vs (b)=+0.900**(estimator +1.000, AUROC 동일)."         # 비교용 baseline 숫자
training_status: idle  # idle | running(위치/GPU 명시) | complete | failed
language: 한국어      # 스킬 출력 언어 — english | 한국어 (값은 영어 유지). 자세히: shared-references/output-language.md
code_dir: codes      # 로컬 코드 디렉토리; OpenFedLLM 은 codes/external/ 참조 클론(reference-guided self-build, gitignored). CNN track 은 자체 시뮬레이터
active_tasks: []     # 백그라운드 작업
next: "**Phase 1 + Phase 2 tasks 1–7 + step-5 BUILD ALL DONE.** **step-5 BUILD(2026-06-09, 이 세션)**: `experiments/phase2_matrix.py`(NEW — 유일 코드변경→CNN guard green) = env-parameterized matrix [REGIME silo5/device100 × THREAT{noisy/freerider_random/freerider_zero/poison} × ALPHA × SEED]; 3 detector(STD-DAGMM/FLTrust/FedDQC) 매 threat; regime-gated method set((b)=exact 2^N silo / `in_run_shapley_perround` device; Banzhaf+exact-(b)는 N=100서 탈락; GTG/FedSV/ShapleyFL/(b)-perround는 COALITION/ORACLE_B 게이트=α0.5 anchor; cheap method는 매 α+Flirds proxy-truth off-anchor); poison=D2b 합성(γ=cohort); task8 scale=SMOKE_MODEL+MODEL_CFG(fp32; 7B small-batch, no bf16). **Forks(Yonghee)**: NEW 파일(검증된 phase1 comparator 보존)+free-rider 두 모드. **검증=5 코드경로 green**(tiny smoke, 값은 coarse): silo5 4-threat(Spearman vs (b)+1.000 전부; poison FLDetector·STD-DAGMM·FedDQC 1.0 vs valuation 0.0=§3.9 framing), device100 cheap(Flirds proxy+1.0, FLTrust1.0, STD-DAGMM0.54, FedDQC0.10 off-threat), device100 anchor(Flirds vs (b)-perround +0.999; GTG+0.93/FedSV+0.82/ShapleyFL+0.86; (b)613s@R4), 3B((b)/Flirds+1.000, no OOM). **⚠ poison 발견(D2b 결론과 충돌→Yonghee 판단+검증세션 #1)**: poison threat는 **full D2b config**(`LR=2e-3 BATCH=8 ROUNDS=10 MAX_STEPS=5 POISON_TRAIN=1000`) 필요 → **ASR=1.00 재현**(matrix 기본 lr1e-3/batch16은 ASR0 — batch16이 attacker install step 절반 189 vs 375). working backdoor서 standard orientation(corrupt=high φ): FLDetector/STD-DAGMM/FLTrust/FedDQC+loss-heur **AUROC1.0**, 그러나 **Flirds-1st&2nd=0.0**(clean-preserving attacker의 γ-update가 clean val-loss 내림→최저 φ=most-helpful→**Flirds 회피**=§3.9 가설 CONFIRM). **D2b는 '회피 REFUTED'**였으나 그건 Flirds AUROC를 −φ로 negate(corrupt=low-value)한 orientation 차이 — attacker는 φ-extreme(two-sided/magnitude/loss-heur catchable)이지 φ-high 아님. §3.9 headline framing(Flirds가 3위협 다 잡음 vs noisy+FR만 잡고 backdoor는 matched detector가 보완)=Yonghee 결정(real config+(b)서 재확인; tiny-config val=20 주의). real-config 재확인 노트: ComFedSV tiny-R Spearman 낮음(task7d=R30서 +1.000), STD-DAGMM AE-CPU~100s, device100 corrupt-seen은 R≈30 필요. 상세: plan §3.9 'step 5 BUILD DONE' + raw [[2026-06-09-phase2-step5-matrix-orchestrator-task8]]. ⟪Prior(2026-06-08):⟫ **Tasks 2–4 완료+검증**(1B N=5 3-seed, RIPPLE=0, 전부 Spearman vs (b)oracle +1.000): **Data Banzhaf**(`baselines/banzhaf.py` = (b)-oracle coalition util을 균등 1/2^{n-1}로 재가중; `in_run_sv._coalition_utilities` 헬퍼 추출, in_run_shapley bit-identical; free-rider φ=정확0; ~531s) · **ShapleyFL**(`baselines/shapleyfl.py` surrogate-FSV = uniform submodel+per-round exact Shapley+min-max+EMA, 논문 정독 재현; DMC estimator→cross-device task7; ~531s) · **loss-heuristic**(singleton in-run util `in_run_utility([k])`; ~164s) · **Flirds-1st-only**(`second_order=False`; **~35s≈15× cheaper**). `phase1_baseline_compare.py` 9-method로 확장(print=method-order 리스트). N=5 near-additive→전부 동일 랭킹→**Flirds 프론티어 지배**(5–15× cheaper). 핵심통찰: Shapley linearity로 exact+우리utility는 (b)oracle과 degenerate-동일 → ShapleyFL의 uniform-util+min-max+EMA가 구별 만듦. **검출 baseline → threat-matched REDESIGN(06-08; 아래 NEXT)**: 옛 FLDetector↔noisy는 위협 불일치(FLDetector=crafted-update poisoning 검출기, answer_swap=정직-나쁜데이터≠그것). **task5 FLDetector DONE+검증**(`baselines/fldetector.py` = model-free server-side from-logs L-BFGS detector: Byrd-Nocedal compact HVP + Cauchy-MVT 예측잔차 ‖ĝ−g‖ ℓ1-norm; **model/loss_fn 불필요→AUROC 표만, Spearman 없음**; gᵢᵗ=raw delta, wᵗ−wᵗ⁻¹=n_c-weighted aggregate, w_r 미사용; ≥1 secant pair부터 score(R<10 대응), Gap+2-means skip. 합성 AUROC=1.0/CNN bit-identical/LLM smoke PORT OK/compare 9→10-method. 1B N=5 3-seed: **최저비용 ~24s**(Flirds-1st 35s보다도 쌈)·**최약 검출** noisy0.50/FR0.75 vs valuation 0.75/1.0 = clean math 매seed 최고 score=non-IID erosion→헤드라인 N=10/100). **task6 (a)-retrain LLM N=5@1B VALIDATED**(06-07; `oracle/exact_sv_llm.py`+`experiments/phase2_llm_a_oracle.py`): (a)-**val-loss**=(b) in-run=estimator **Spearman +1.000**(fp32, 두 lr). 핵심교훈(Yonghee): (a)는 method 검증 위해 **val-loss(같은 게임)** 써야 — ROUGE는 다른 게임(미분불가→estimator-ROUGE 불가능); 약했던 건 **bf16 정밀도**(val-loss 차이~0.005-0.02 < bf16 prec 0.009 = (b) fp32 이유)지 신호크기 아님. ROUGE-divergence(기억용): (a)ROUGE vs (b) +0.4(1B)/−0.9(3B)=answer_swap 도메인-포맷에 속음, val-loss는 안 속음. cost ladder(a N=5): 47min(1B bf16)/126min(1B fp32)/90min(3B bf16); **N=10=retrain 64×+eval 32×→2-5일/1-GPU→실제 실험서 멀티-GPU 샤딩**. 3B (a)-valloss fp32 백그라운드 confirm 중. **task7a-7d DONE(06-08, 1B)**: **7a** `fl.partition.client_dirichlet_partition`+`data.llm.build_crossdevice`(per-CLIENT Dir(α) 도메인-혼합=**Option B**; 기존 per-class `dirichlet_partition`은 5도메인→100클라서 degenerate[α=0→5클라만,크기 0–12k]; Option B=고정크기·전부 non-empty·α=0=domain-disjoint·purity==A) · **7b** N=100 Flirds 검증완료(라이브러리 무변경; `sample_frac=0.1`→K=10, `flirds_values(n_clients=100)` 명시; FR φ exact-0) · **7c (b)oracle=EXACT per-round 분해**(φ_i=Σ_{r:i∈P_r} 2^{|P_r|}-Shapley = 2^N oracle, Δφ≈3e-16 증명; **MC 아님** — Yonghee: MC는 (a)-RETRAIN용, in-run은 싸서 exact): N=100 α=0.5 **Flirds vs (b) Spearman +1.000**, oracle **771ms/fwd**(fp32-B200)→~11h/4-GPU→1-2 α만 · **7d** ComFedSV LLM 포팅(`comfedsv_from_logs(loss_fn,pkeys)`, uniform-subset, partial=True; ==exact uniform-Shapley +1.000; CNN bit-identical). **task7e steps 1–3 DONE(06-08, code+검증; 미커밋 후 commit→Yonghee push)**: **STD-DAGMM**(`baselines/std_dagmm.py` model-free DAGMM+std, per-(client,round) pooling+signed feature-hash 5.6M→256, std는 full벡터): synthetic AUROC=1.0(zero=std feature, std-matched random=recon/cosine); real 1B N=100 free-rider **AUROC=0.628**=pure-evasion case서 model-free 검출기 LLM-scale 약함(정직; synthetic이 로직 증명). `llm_server.free_rider_scale` thread(benign-std 튜닝, uniform std=s/√3). **FLTrust**(`baselines/fltrust.py`, g0=−∇_val(w_r)→**normalized Flirds-1st**, cosine≈Flirds-1st **exact**; **signed cosine, NOT ReLU**[ReLU는 benign<0+free-rider~0 둘 다 0으로 뭉개 free-rider 검출 깨뜨림; ReLU/mag-norm은 aggregation gate]): unit AUROC free-rider+poison=1.0; real 1B N=100 free-rider **AUROC=1.0**(top-5), Spearman vs Flirds-1st=0.60. 통찰: gradient-쓰는 검출기(FLTrust≈Flirds-1st)는 free-rider 완벽(1.0), model-free STD-DAGMM은 약함(0.63)→**gradient가 결정적**; Flirds가 FLTrust 포섭, STD-DAGMM이 독립(약)free-rider baseline. **poisoning code**: `corruptors.backdoor()`(Xu trigger→target, poison_frac=clean-preservation knob)+`backdoor=`/`backdoor_kwargs=` 주입(build/build_crossdevice); `llm_server.scaled_attackers`/`attack_scale`(Bagdasaryan ×γ model-replacement); `generate.backdoor_asr`. **FLDetector poisoning 재조준+cross-device 적응=gap-integrated HVP(Yonghee 선택 option a)**: client 직전참여 t'에서 gap w^r−w^{t'} 예측, gap당 HVP 1회 캐시—**full participation서 bit-identical**(CNN guard green)+cross-device synthetic AUROC=1.0. 검출(real-1B N=5 scaled attacker): FLDetector·FLTrust **AUROC=1.0**; **Flirds-1st 판정이 poison_frac으로 뒤집힘**(0.5 clean-helpful→evade AUROC0; 1.0 순수backdoor→flag AUROC1; γ scaling이 판정 증폭)=clean-preservation 경계, **보고만 pre-position 금지**. **task7e RESOLVED(06-09 직전세션)**: backdoor-install=under-training이었음 — **D1**(no-FL install isolation: frac0.5–0.8=clean-preserving install[triggered-ASR~1.0, clean-ASR0], single-token target; frac1.0=clean파괴; ≤0.2=미달) / **D2**(FL model-replacement: full-repl γ=n/η=5 전파[ASR0.97, clean-val+0.027]; partial/norm-bound=0=all-or-nothing; attacker‖Δ‖=40×benign→stealthy arm 불가) / **D2b**(working backdoor→FLDetector·FLTrust·Flirds-1st/2nd 전부 AUROC1.0='clean-preserving evades Flirds' 그 config선 반증; 경계=matrix). **step4 FedDQC**(`baselines/feddqc.py`, IRA=L(a)−L(a|q)→−IRA; noisy AUROC1.0). **검출 suite 완성**(data-quality→FedDQC / free-rider→STD-DAGMM+FLTrust / poisoning→FLDetector+FLTrust+Flirds). backdoor 논문 ingested(web; PDF 미보유). **step5 grid+실행전략 LOCKED**: detection cross-silo N5(all+(b) Spearman)+cross-device N100 α-sweep{0,0.01,0.1,0.5,5.0}((b)@α0.5만); Ripple 제외; seed3; cost-tiered stage-gate(N5→N100→3B→7B, category-together, (b)=anchor, adaptive). **NEXT(이 세션 후): (1) 독립 VERIFICATION 세션(Yonghee 요청—아래 프롬프트) → (2) real grid 실행(cost-tiered: silo5 N5→device100 α-sweep+α0.5 anchor→3B→7B; poison threat는 LR=2e-3).** · task8 3B/7B(7B bf16train/fp32eval); task9(corruptor)는 7e에 흡수 → Phase 3. ✅ **3B (a)-valloss fp32 CONFIRMED(06-08)**: vs (b)=+0.900(clean-client 1-swap=retrain noise), estimator=+1.000, AUROC 동일(noisy0.75/FR1.0); (a)ROUGE=+0.100(속음). 1B 검증이 3B서 유지. 상세: plan 'Next concrete action(2026-06-08)' + §3.9(재설계) + `raw/conversations/flirds/2026-06-08-phase2-task7-crossdevice-detection-redesign`. ⚠ git: main이 origin보다 앞섬(미푸시)→Yonghee push. ⚠ Ripple eigsh flaky(이번 smoke서 CPU spinning 멈춤; 알려진 수렴 이슈)→compare는 RIPPLE=0(Ripple 값은 06-06것). 환경: python `/home/korea_bupj/miniconda3/envs/flirds/bin/python`, GPU 0-3만, `codes/`서 PYTHONPATH=."  # 다음 단계
last_updated: "2026-06-09"  # YYYY-MM-DD
```

## 프로젝트 제약

- (미정 — 추후 채워주세요)

## 비목표 (Non-Goals)

- (미정)

## 컴퓨팅 예산

- Nvidia DGX B200 x4

<!-- KARIS:BEGIN -->
## KARIS Skill Scope
KARIS skills installed in this project: 55 entries.
Manifest: `.karis/installed-skills.txt` (lists every skill KARIS installed and its upstream target).
For KARIS workflows, prefer the project-local skills under `.claude/skills/` over global skills.
Do not modify or delete files inside any skill that is a symlink (symlinks point into `/NHNHOME/WORKSPACE/26msit001_A/edge_ai_lab/yonghee/karis`).
Update with: `bash /NHNHOME/WORKSPACE/26msit001_A/edge_ai_lab/yonghee/karis/tools/install_karis.sh`  (re-runnable; reconciles new/removed skills).
<!-- KARIS:END -->
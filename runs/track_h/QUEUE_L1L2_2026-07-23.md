# L1+L2 큐 — 다음 B200 컨테이너용 (2026-07-23)

> Yonghee 07-23 우선순위: **현 큐의 β-이전 실험까지 완주 → L1·L2(본 문서) → β0.3 재개**.
> 스펙·예산·전체 캠페인 = `REMAINING.md` **§1.6** + `runs/track_h/README.md` H-12·H-13
> (사전등록 완료). env 베이스 = `REMAINING.md` §1 컨테이너 공통 블록(PY/HOME/HF_* 동일).

## 0. 사전 절차 (컨테이너 기동 직후, GPU 불요)

1. **코드 동기화 + git clean 확인** — 신규 rundir meta의 git_sha가 fix(`8598cea`)+revert(`fa2c167`)
   이후 커밋이어야 canonical(커밋 후 제출 관례).
2. **P-2 hf_pin**: `codes/flirds/hf_pin.py` REVISIONS를 `$BATCH/hf_home` 캐시의
   `models--*/snapshots/<sha>`·`datasets--*/snapshots/<sha>` 디렉토리명으로 채움(오프라인 캐시가
   곧 정본; gated meta-llama 포함 — PROVENANCE.md 체인과 정합). 커밋.
3. **pre-fix Tier A 아카이브**: `runs/track_h/rundirs_llm/` 중 **meta.json git_sha==`fa5fc6e`인
   rundir만** `rundirs_llm_prefixh1/`로 이동(동결 보존 — P-1 판정; 07-22 P5-soft 신런은 git_sha가
   다르므로 자동 제외됨). 재실행 셀명 충돌(해시 접미사 오염) 방지 목적. 이동도 커밋.

## 1. L2 스모크 (수 분, GPU 1장)

```bash
PERSIST=0 ROUNDS=3 VAL=20 MAX_STEPS=2 REGIME=gsm50k5 THREAT=noisy SEED=0 \
  CUDA_VISIBLE_DEVICES=0 PYTHONPATH=. $PY -u experiments/phase2_matrix.py
```
확인: (b)oracle 포함 전 방법 행 출력 + Spearman/(b) 산출 + 오류 0. (PERSIST=0 = rundir 미생성.)

## 2. L1 — R4 Tier C 재실행 (noisy·frzero × seeds 0·1·2; ~120–138 GPU-h)

- 레시피 = **Tier A와 동일**($BATCH/runlogs의 R4 큐 파일 재사용, SEED만 {0,1,2} 치환;
  arm 세트: observer + T1 flirds(gate_v2·pweight) + T2 4점수원 + 통제 random_k/oracle_excl/random_excl).
- **개선 1건(REMAINING §1.0 교훈)**: T2 4점수원 재학습은 서로 독립 — 한 프로세스 직렬(소스당 ~3h)
  대신 **소스별 프로세스로 큐 분할**(wall 13.7h→~8h/셀).
- 배치: seed-major로 4 GPU 분산. frzero는 kept-동일 dedupe 시 셀당 ~15.8 GPU-h.
- 완료 후: `python runs/track_h/make_analysis.py` → H-12 자동 대조(pre-fix 대비 순위 재현) →
  rundir+분석 커밋.

## 3. L2 — R4 (b)-fidelity 셀 (noisy → clean; 셀당 ~10–15 GPU-h, GPU 1장씩)

```bash
# noisy (nr 기본 0.7; ORACLE_B/COALITION 기본 on) -> rundir 1B_gsm50k5_noisy_nr0.7_s0
REGIME=gsm50k5 THREAT=noisy SEED=0 CUDA_VISIBLE_DEVICES=<g> PYTHONPATH=. $PY -u experiments/phase2_matrix.py
# clean (포화 대조) -> 1B_gsm50k5_clean_s0
REGIME=gsm50k5 THREAT=clean SEED=0 CUDA_VISIBLE_DEVICES=<g> PYTHONPATH=. $PY -u experiments/phase2_matrix.py
```
- rundir 루트 = 기본 `runs/phase2_matrix/rundirs/`(러너 소속 관례). frzero 셀은 예산 여유 시만
  ((b)-frzero는 exact-0 자명 — H-13 스코프 밖).
- 완료 후: metrics.json 스팟(Spearman 표 = H-13 대조) → rundir 커밋. fidelity.csv 롤업은
  분석 단계에서 c2fid 열-호환 스키마로(+stage 컬럼) — CNN fidelity leg와 공용 표 생성.

## 4. 이후 순서

1. **L4 게이트(Day 2)**: Tier B T2-only(renorm 4점수원 × noisy·frzero, ~150 GPU-h) — Yonghee 승인 시.
2. 여유 시 L5(비등n silo5)·L6(silo5 graded-noisy) — REMAINING §1.6.
3. **β0.3 재개**(`runs/rerun_beta03/RESUME_AFTER_MIGRATION.md`) — poison 2셀은 큐에서 제외 가능
   (논문 제외 축). 착지 rundir git_sha 확인.

## 체크리스트 (실행 규율)

- [ ] 신규 rundir 전부 meta git_sha = fix-후 커밋 (git clean)
- [ ] H-12·H-13 사전등록 행이 실행 전에 커밋돼 있음 (본 문서와 동일 커밋)
- [ ] 결과 수치는 rundir + make_analysis 재생성만 (수기 기입 금지)
- [ ] push는 Yonghee 직접

# ════════════════════════════════════════════════════════════════════════════
# B200 **세션 A** / 컨테이너 1 (GPU 1장) — G1 noisy 레인
#   가동: CID=1 bash <repo>/runs/track_h/run_b200_batch.sh
#   형식: SCRIPT|LOGNAME|envs     ·  __REPO__ = 런처가 sed 치환
#   ⚠ 완료 줄은 '#' **주석**(줄 삭제 금지 — 드라이버가 consumed 인덱스로 추적)
#   ⚠ 가동 후 줄 순서 변경 금지 · 정지는 드레인(실행 중 셀 완주 후 종료)
#
# 배치(2026-07-25 스코프 컷 반영):  세션 A = CID 1(noisy) + CID 2(clean)
#                                  세션 B = CID 3(frzero) + CID 4(L1 clean·G5·G12)
# **R4 = R=200 유지.**  LLM downstream 을 {vanilla·oracle·random·flirds류}로 줄이면서
# L11·G4c 가 빠졌고, 그 둘을 넣으려던 R=100 전환도 함께 철회됐다 → 이미 완결·커밋된
# L1 3-seed 를 재실행 없이 그대로 쓴다.  ROUNDS 를 주지 않는다(레짐 기본 200).
#
# ⚠ G1 은 셀당 ~19.6h 이고 `phase2_matrix` 는 **cell-end 1회 persist** 다 —
#   컨테이너가 중도에 끊기면 그 셀은 전손이다(직전 컨테이너 실사례).
#   컷이 예상되면 신규 투입을 먼저 끊고 드레인할 것(seal watchdog 운용 권장).
# ════════════════════════════════════════════════════════════════════════════

# ─── G1 : R4-L2 주무대 (b) 오라클 · noisy(=answer-swap@0.7) ─────────────────
# 한 rundir 이 §5.2 fidelity + §5.4 탐지 + §5.5 canonical timing 세 축을 동시에 연다.
# THREAT 토큰은 phase2_matrix 규약(clean|noisy|freerider_zero) — track_g 의 frzero 와 다름.
phase2_matrix.py|G1_L2_noisy_s0|RUN_NAME= REGIME=gsm50k5 THREAT=noisy SEED=0
phase2_matrix.py|G1_L2_noisy_s1|RUN_NAME= REGIME=gsm50k5 THREAT=noisy SEED=1
phase2_matrix.py|G1_L2_noisy_s2|RUN_NAME= REGIME=gsm50k5 THREAT=noisy SEED=2
# 이 컨테이너는 G1 noisy 전용이다(배치 규약: c1/c2/c3 = G1, c4 = 나머지 전부).
# 완주 시각 ≈ 58.8h.  남는 창(~15h)은 컷·재시도 마진으로 비워 둔다 —
# G1 은 셀당 19.6h 이고 cell-end 1회 persist 라 여유가 곧 보험이다.
